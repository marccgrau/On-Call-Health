---
phase: 04-frontend-ui-ux
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/components/api-keys/ApiKeyList.tsx
  - frontend/src/components/api-keys/RevokeKeyDialog.tsx
  - frontend/src/app/dashboard/api-keys/page.tsx
autonomous: false

must_haves:
  truths:
    - "User sees list of API keys in table format"
    - "User sees masked key display (och_live_****1234)"
    - "User sees key metadata (name, created, last used, expires)"
    - "User can click Revoke on a key"
    - "User sees confirmation dialog before revocation"
    - "Key disappears from list after revocation"
  artifacts:
    - path: "frontend/src/components/api-keys/ApiKeyList.tsx"
      provides: "Grid-based table for API keys"
      min_lines: 60
    - path: "frontend/src/components/api-keys/RevokeKeyDialog.tsx"
      provides: "Revocation confirmation dialog"
      min_lines: 40
      contains: "cannot be undone"
  key_links:
    - from: "frontend/src/components/api-keys/RevokeKeyDialog.tsx"
      to: "useApiKeys.revokeKey"
      via: "function prop"
      pattern: "onConfirmRevoke"
    - from: "frontend/src/app/dashboard/api-keys/page.tsx"
      to: "ApiKeyList"
      via: "component import and render"
      pattern: "ApiKeyList"
---

<objective>
Create the API key list display and revocation flow with confirmation dialog.

Purpose: Allow users to view their existing API keys in a table (with masked display) and revoke keys with proper confirmation to prevent accidental deletion.

Output: ApiKeyList component showing keys in grid format, RevokeKeyDialog for confirmation, integrated into API Keys page with human verification.
</objective>

<execution_context>
@/Users/sylvainkalache/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sylvainkalache/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-ui-ux/04-CONTEXT.md
@.planning/phases/04-frontend-ui-ux/04-RESEARCH.md
@.planning/phases/04-frontend-ui-ux/04-01-SUMMARY.md
@.planning/phases/04-frontend-ui-ux/04-02-SUMMARY.md

# Existing patterns to follow
@frontend/src/app/integrations/dialogs/DeleteIntegrationDialog.tsx
@frontend/src/app/integrations/dialogs/OrganizationManagementDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the ApiKeyList component</name>
  <files>frontend/src/components/api-keys/ApiKeyList.tsx</files>
  <action>
Create frontend/src/components/api-keys/ApiKeyList.tsx following the grid-based table pattern from OrganizationManagementDialog:

```typescript
"use client"

import { formatDistanceToNow, format } from "date-fns"
import { Button } from "@/components/ui/button"
import { Badge } from "@/components/ui/badge"
import { Trash2 } from "lucide-react"
import { ApiKey } from "@/types/apiKey"

interface ApiKeyListProps {
  keys: ApiKey[]
  onRevokeClick: (key: ApiKey) => void
}

function formatDate(dateString: string | null): string {
  if (!dateString) return "Never"
  const date = new Date(dateString)
  return format(date, "MMM d, yyyy")
}

function formatLastUsed(dateString: string | null): string {
  if (!dateString) return "Never"
  const date = new Date(dateString)
  return formatDistanceToNow(date, { addSuffix: true })
}

function getExpirationStatus(expiresAt: string | null): { text: string; variant: "default" | "secondary" | "destructive" | "outline" } {
  if (!expiresAt) {
    return { text: "Never expires", variant: "secondary" }
  }
  const expDate = new Date(expiresAt)
  const now = new Date()
  const daysUntilExpiry = Math.ceil((expDate.getTime() - now.getTime()) / (1000 * 60 * 60 * 24))

  if (daysUntilExpiry < 0) {
    return { text: "Expired", variant: "destructive" }
  } else if (daysUntilExpiry <= 7) {
    return { text: `Expires in ${daysUntilExpiry} day${daysUntilExpiry !== 1 ? 's' : ''}`, variant: "destructive" }
  } else if (daysUntilExpiry <= 30) {
    return { text: `Expires in ${daysUntilExpiry} days`, variant: "outline" }
  } else {
    return { text: formatDate(expiresAt), variant: "secondary" }
  }
}

export function ApiKeyList({ keys, onRevokeClick }: ApiKeyListProps) {
  return (
    <div className="border rounded-lg overflow-hidden">
      {/* Header Row */}
      <div className="bg-neutral-100 px-4 py-3 border-b">
        <div className="grid grid-cols-12 gap-4 text-sm font-medium text-neutral-700">
          <div className="col-span-3">Name</div>
          <div className="col-span-3">Key</div>
          <div className="col-span-2">Created</div>
          <div className="col-span-2">Last Used</div>
          <div className="col-span-1">Expires</div>
          <div className="col-span-1 text-right">Actions</div>
        </div>
      </div>

      {/* Data Rows */}
      <div className="max-h-96 overflow-y-auto">
        {keys.map((key) => {
          const expStatus = getExpirationStatus(key.expires_at)
          return (
            <div
              key={key.id}
              className="px-4 py-3 border-b last:border-b-0 hover:bg-neutral-50 transition-colors"
            >
              <div className="grid grid-cols-12 gap-4 text-sm items-center">
                {/* Name */}
                <div className="col-span-3 font-medium text-neutral-900 truncate" title={key.name}>
                  {key.name}
                </div>

                {/* Masked Key */}
                <div className="col-span-3">
                  <code className="font-mono text-xs bg-neutral-100 px-2 py-1 rounded text-neutral-700">
                    och_live_****{key.last_four}
                  </code>
                </div>

                {/* Created */}
                <div className="col-span-2 text-neutral-600">
                  {formatDate(key.created_at)}
                </div>

                {/* Last Used */}
                <div className="col-span-2 text-neutral-600">
                  {formatLastUsed(key.last_used_at)}
                </div>

                {/* Expires */}
                <div className="col-span-1">
                  <Badge variant={expStatus.variant} className="text-xs whitespace-nowrap">
                    {expStatus.text}
                  </Badge>
                </div>

                {/* Actions */}
                <div className="col-span-1 text-right">
                  <Button
                    variant="ghost"
                    size="sm"
                    onClick={() => onRevokeClick(key)}
                    className="text-red-600 hover:text-red-700 hover:bg-red-50"
                  >
                    <Trash2 className="w-4 h-4" />
                    <span className="sr-only">Revoke {key.name}</span>
                  </Button>
                </div>
              </div>
            </div>
          )
        })}
      </div>
    </div>
  )
}
```

Key implementation notes:
- Use 12-column grid layout (matches existing patterns)
- Masked key format: `och_live_****{last_four}`
- formatDistanceToNow for "Last Used" (e.g., "2 hours ago")
- format for "Created" date (e.g., "Jan 30, 2026")
- Expiration badges with color-coded status:
  - Expired or <7 days: destructive (red)
  - <30 days: outline (border)
  - Never/far future: secondary (gray)
- Trash icon button for revoke action
- max-h-96 with overflow-y-auto for scrollable list
  </action>
  <verify>
```bash
cd frontend && npx tsc --noEmit
```
  </verify>
  <done>
ApiKeyList.tsx exists with grid-based table, masked key display, date formatting, expiration badges, and revoke buttons.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the RevokeKeyDialog component</name>
  <files>frontend/src/components/api-keys/RevokeKeyDialog.tsx</files>
  <action>
Create frontend/src/components/api-keys/RevokeKeyDialog.tsx following the DeleteIntegrationDialog pattern:

```typescript
"use client"

import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog"
import { Button } from "@/components/ui/button"
import { AlertTriangle, Loader2 } from "lucide-react"
import { ApiKey } from "@/types/apiKey"

interface RevokeKeyDialogProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  keyToRevoke: ApiKey | null
  isRevoking: boolean
  onConfirmRevoke: () => void
  onCancel: () => void
}

export function RevokeKeyDialog({
  open,
  onOpenChange,
  keyToRevoke,
  isRevoking,
  onConfirmRevoke,
  onCancel,
}: RevokeKeyDialogProps) {
  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="sm:max-w-md">
        <DialogHeader>
          <DialogTitle className="flex items-center gap-2 text-red-700">
            <AlertTriangle className="w-5 h-5" />
            Revoke API Key
          </DialogTitle>
          <DialogDescription>
            Are you sure you want to revoke this API key?
          </DialogDescription>
        </DialogHeader>

        <div className="py-4">
          {/* Warning */}
          <div className="p-3 bg-red-50 border border-red-200 rounded-lg mb-4">
            <p className="text-sm text-red-800">
              <strong>This action cannot be undone.</strong> Any applications using this key will
              immediately lose access to MCP endpoints.
            </p>
          </div>

          {/* Key details */}
          {keyToRevoke && (
            <div className="p-3 bg-neutral-50 border border-neutral-200 rounded-lg">
              <div className="text-sm space-y-1">
                <p>
                  <span className="font-medium text-neutral-700">Name:</span>{" "}
                  <span className="text-neutral-900">{keyToRevoke.name}</span>
                </p>
                <p>
                  <span className="font-medium text-neutral-700">Key:</span>{" "}
                  <code className="font-mono text-xs bg-neutral-200 px-1.5 py-0.5 rounded">
                    och_live_****{keyToRevoke.last_four}
                  </code>
                </p>
              </div>
            </div>
          )}
        </div>

        <DialogFooter>
          <Button variant="outline" onClick={onCancel} disabled={isRevoking}>
            Cancel
          </Button>
          <Button
            variant="destructive"
            onClick={onConfirmRevoke}
            disabled={isRevoking}
            className="bg-red-600 hover:bg-red-700"
          >
            {isRevoking ? (
              <>
                <Loader2 className="w-4 h-4 mr-2 animate-spin" />
                Revoking...
              </>
            ) : (
              "Revoke Key"
            )}
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  )
}
```

Key implementation notes:
- Follow DeleteIntegrationDialog pattern exactly
- Red/danger color scheme for destructive action
- Show key name and masked key for confirmation
- Clear warning: "This action cannot be undone"
- Explain consequence: "applications will immediately lose access"
- Disable buttons while revoking
- Loading spinner in button during revocation
  </action>
  <verify>
```bash
cd frontend && npx tsc --noEmit
```
  </verify>
  <done>
RevokeKeyDialog.tsx exists with warning, key details, and destructive button styling.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate list and revoke dialog into API Keys page</name>
  <files>frontend/src/app/dashboard/api-keys/page.tsx</files>
  <action>
Update frontend/src/app/dashboard/api-keys/page.tsx to integrate the list and revocation flow:

```typescript
"use client"

import { useState } from "react"
import { Key, Plus, Loader2 } from "lucide-react"
import { Button } from "@/components/ui/button"
import { useApiKeys } from "@/hooks/useApiKeys"
import { CreateKeyDialog } from "@/components/api-keys/CreateKeyDialog"
import { KeyCreatedDialog } from "@/components/api-keys/KeyCreatedDialog"
import { ApiKeyList } from "@/components/api-keys/ApiKeyList"
import { RevokeKeyDialog } from "@/components/api-keys/RevokeKeyDialog"
import { ApiKey, CreateApiKeyResponse } from "@/types/apiKey"

export default function ApiKeysPage() {
  const { keys, loading, error, createKey, revokeKey } = useApiKeys()

  // Create flow state
  const [showCreateDialog, setShowCreateDialog] = useState(false)
  const [showSuccessDialog, setShowSuccessDialog] = useState(false)
  const [createdKey, setCreatedKey] = useState<CreateApiKeyResponse | null>(null)

  // Revoke flow state
  const [showRevokeDialog, setShowRevokeDialog] = useState(false)
  const [keyToRevoke, setKeyToRevoke] = useState<ApiKey | null>(null)
  const [isRevoking, setIsRevoking] = useState(false)

  // Create handlers
  const handleKeyCreated = (response: CreateApiKeyResponse) => {
    setCreatedKey(response)
    setShowSuccessDialog(true)
  }

  const handleSuccessDialogClose = () => {
    setShowSuccessDialog(false)
    setCreatedKey(null)
  }

  // Revoke handlers
  const handleRevokeClick = (key: ApiKey) => {
    setKeyToRevoke(key)
    setShowRevokeDialog(true)
  }

  const handleRevokeCancel = () => {
    setShowRevokeDialog(false)
    setKeyToRevoke(null)
  }

  const handleConfirmRevoke = async () => {
    if (!keyToRevoke) return

    setIsRevoking(true)
    try {
      const success = await revokeKey(keyToRevoke.id)
      if (success) {
        setShowRevokeDialog(false)
        setKeyToRevoke(null)
      }
    } finally {
      setIsRevoking(false)
    }
  }

  return (
    <div className="min-h-screen bg-neutral-100">
      <div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="flex items-center justify-between mb-8">
          <div>
            <h1 className="text-2xl font-bold text-neutral-900 flex items-center gap-2">
              <Key className="w-6 h-6" />
              API Keys
            </h1>
            <p className="text-sm text-neutral-600 mt-1">
              Manage API keys for programmatic access to MCP endpoints
            </p>
          </div>
          <Button
            onClick={() => setShowCreateDialog(true)}
            className="bg-purple-700 hover:bg-purple-800"
          >
            <Plus className="w-4 h-4 mr-2" />
            Create API Key
          </Button>
        </div>

        {/* Content */}
        <div className="bg-white rounded-lg border border-neutral-200 shadow-sm">
          {loading ? (
            <div className="flex items-center justify-center py-16">
              <Loader2 className="w-8 h-8 animate-spin text-purple-600" />
              <span className="ml-3 text-neutral-600">Loading API keys...</span>
            </div>
          ) : error ? (
            <div className="text-center py-16">
              <p className="text-red-600">{error}</p>
            </div>
          ) : keys.length === 0 ? (
            <div className="text-center py-16">
              <Key className="w-12 h-12 mx-auto mb-4 text-neutral-400" />
              <h3 className="text-lg font-medium text-neutral-900 mb-2">No API keys yet</h3>
              <p className="text-neutral-600 mb-4">
                Create your first API key to access MCP endpoints programmatically.
              </p>
              <Button
                onClick={() => setShowCreateDialog(true)}
                className="bg-purple-700 hover:bg-purple-800"
              >
                <Plus className="w-4 h-4 mr-2" />
                Create API Key
              </Button>
            </div>
          ) : (
            <ApiKeyList keys={keys} onRevokeClick={handleRevokeClick} />
          )}
        </div>

        {/* Security note */}
        <div className="mt-6 p-4 bg-amber-50 border border-amber-200 rounded-lg">
          <p className="text-sm text-amber-800">
            <strong>Security note:</strong> API keys provide full access to your MCP endpoints.
            Keep them secure and never share them publicly.
          </p>
        </div>
      </div>

      {/* Create Key Dialog */}
      <CreateKeyDialog
        open={showCreateDialog}
        onOpenChange={setShowCreateDialog}
        onCreateKey={createKey}
        onKeyCreated={handleKeyCreated}
      />

      {/* Key Created Success Dialog */}
      <KeyCreatedDialog
        open={showSuccessDialog}
        onOpenChange={handleSuccessDialogClose}
        createdKey={createdKey}
      />

      {/* Revoke Confirmation Dialog */}
      <RevokeKeyDialog
        open={showRevokeDialog}
        onOpenChange={(open) => {
          if (!open && !isRevoking) {
            handleRevokeCancel()
          }
        }}
        keyToRevoke={keyToRevoke}
        isRevoking={isRevoking}
        onConfirmRevoke={handleConfirmRevoke}
        onCancel={handleRevokeCancel}
      />
    </div>
  )
}
```

The complete flow now:
1. Page loads and fetches keys via useApiKeys hook
2. Keys displayed in ApiKeyList grid table
3. User clicks revoke icon -> setKeyToRevoke, showRevokeDialog = true
4. User confirms in RevokeKeyDialog -> revokeKey() called
5. On success, dialog closes and list refreshes automatically (via useApiKeys.fetchKeys)
  </action>
  <verify>
1. TypeScript compiles:
```bash
cd frontend && npx tsc --noEmit
```

2. Check imports:
```bash
grep -q "ApiKeyList" frontend/src/app/dashboard/api-keys/page.tsx
grep -q "RevokeKeyDialog" frontend/src/app/dashboard/api-keys/page.tsx
```
  </verify>
  <done>
API Keys page integrates ApiKeyList and RevokeKeyDialog with complete revocation flow.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete API key management UI:
- Navigation link in user dropdown
- API Keys page at /dashboard/api-keys
- Key creation flow (form -> success with copy)
- Key list with masked display
- Key revocation with confirmation
  </what-built>
  <how-to-verify>
1. Start the frontend dev server:
   ```bash
   cd frontend && npm run dev
   ```

2. Navigate to the app and sign in (if required)

3. Click user avatar -> verify "API Keys" menu item exists

4. Click "API Keys" -> verify page loads at /dashboard/api-keys

5. If no keys exist, verify empty state is shown

6. Click "Create API Key":
   - Enter a name (e.g., "Test Key")
   - Select an expiration (or leave as "No expiration")
   - Click "Create Key"
   - Verify success dialog shows full key with security warning
   - Verify "Copy" button works and shows "Copied!" feedback
   - Click "Done"

7. Verify key appears in list with:
   - Name displayed
   - Masked key (och_live_****XXXX)
   - Created date
   - Last used (should be "Never")
   - Expiration badge

8. Click the trash icon to revoke:
   - Verify confirmation dialog shows key name and masked key
   - Verify warning about action being permanent
   - Click "Revoke Key"
   - Verify key disappears from list
   - Verify success toast appears

9. Test edge cases:
   - Try creating key with empty name (should show validation error)
   - Try creating key with very long name (should work or show error)
   - Verify page is responsive on mobile viewport
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript compilation passes:
   ```bash
   cd frontend && npx tsc --noEmit
   ```

2. All component files exist:
   ```bash
   ls -la frontend/src/components/api-keys/ApiKeyList.tsx
   ls -la frontend/src/components/api-keys/RevokeKeyDialog.tsx
   ```

3. Grid layout used:
   ```bash
   grep -q "grid-cols-12" frontend/src/components/api-keys/ApiKeyList.tsx
   ```

4. Warning text exists:
   ```bash
   grep -q "cannot be undone" frontend/src/components/api-keys/RevokeKeyDialog.tsx
   ```

5. Page imports all components:
   ```bash
   grep -q "ApiKeyList" frontend/src/app/dashboard/api-keys/page.tsx
   grep -q "RevokeKeyDialog" frontend/src/app/dashboard/api-keys/page.tsx
   grep -q "CreateKeyDialog" frontend/src/app/dashboard/api-keys/page.tsx
   grep -q "KeyCreatedDialog" frontend/src/app/dashboard/api-keys/page.tsx
   ```
</verification>

<success_criteria>
- [ ] ApiKeyList renders keys in grid-based table format
- [ ] Masked key displayed as och_live_****{last_four}
- [ ] Created date formatted nicely
- [ ] Last used shows relative time (e.g., "2 hours ago")
- [ ] Expiration shows badge with color-coded status
- [ ] Revoke button on each row opens confirmation dialog
- [ ] RevokeKeyDialog shows key name and masked key
- [ ] RevokeKeyDialog has clear warning about permanent action
- [ ] Revoke button in dialog shows loading state
- [ ] After revocation, key disappears from list
- [ ] Human verification confirms complete UI flow works
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-ui-ux/04-03-SUMMARY.md`
</output>

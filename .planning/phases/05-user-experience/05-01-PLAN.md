---
phase: 05-user-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/integrations/dialogs/AuthMethodSwitchDialog.tsx
  - frontend/src/app/integrations/components/JiraConnectedCard.tsx
  - frontend/src/app/integrations/components/LinearConnectedCard.tsx
  - frontend/src/app/integrations/components/AuthMethodBadge.tsx
autonomous: true

must_haves:
  truths:
    - "User sees clearly which auth method (OAuth vs API Token) is used on connected integration cards"
    - "User sees a 'Switch to X' button on connected cards to initiate method switching"
    - "Switch confirmation dialog shows data preservation message to reassure users"
  artifacts:
    - path: "frontend/src/app/integrations/components/AuthMethodBadge.tsx"
      provides: "Standalone auth method badge component with OAuth/API Token variants"
      exports: ["AuthMethodBadge"]
    - path: "frontend/src/app/integrations/dialogs/AuthMethodSwitchDialog.tsx"
      provides: "Reusable switch confirmation dialog with data preservation message"
      exports: ["AuthMethodSwitchDialog"]
    - path: "frontend/src/app/integrations/components/JiraConnectedCard.tsx"
      provides: "Jira card with auth method badge and switch button"
      contains: "Switch to"
    - path: "frontend/src/app/integrations/components/LinearConnectedCard.tsx"
      provides: "Linear card with auth method badge and switch button"
      contains: "Switch to"
  key_links:
    - from: "JiraConnectedCard.tsx"
      to: "AuthMethodBadge.tsx"
      via: "import and render AuthMethodBadge"
      pattern: "import.*AuthMethodBadge"
    - from: "LinearConnectedCard.tsx"
      to: "AuthMethodBadge.tsx"
      via: "import and render AuthMethodBadge"
      pattern: "import.*AuthMethodBadge"
---

<objective>
Create auth method visibility components and switch UI elements for Jira and Linear connected cards.

Purpose: Enable users to clearly see their current auth method and initiate switching to the alternative method.
Output: AuthMethodBadge component, AuthMethodSwitchDialog component, updated connected cards with switch buttons.
</objective>

<execution_context>
@/Users/sylvainkalache/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sylvainkalache/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-user-experience/05-CONTEXT.md
@.planning/phases/05-user-experience/05-RESEARCH.md
@frontend/src/app/integrations/components/JiraConnectedCard.tsx
@frontend/src/app/integrations/components/LinearConnectedCard.tsx
@frontend/src/app/integrations/components/StatusIndicator.tsx
@frontend/src/app/integrations/dialogs/JiraDisconnectDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AuthMethodBadge and AuthMethodSwitchDialog components</name>
  <files>
    frontend/src/app/integrations/components/AuthMethodBadge.tsx
    frontend/src/app/integrations/dialogs/AuthMethodSwitchDialog.tsx
  </files>
  <action>
    Create AuthMethodBadge component:
    - Accept authMethod prop ("oauth" | "manual")
    - OAuth badge: blue-100 background, blue-700 text, RefreshCw icon (indicates auto-renew)
    - API Token badge: neutral-200 background, neutral-700 text, Key icon
    - Use Badge from @/components/ui/badge
    - Include icon (w-3 h-3) and text label

    Create AuthMethodSwitchDialog component:
    - Props: open, onOpenChange, fromMethod, toMethod, integrationName, isDisconnecting, onConfirmSwitch
    - Title: "Switch to {toLabel}?"
    - Description: Explain disconnect/reconnect process
    - Data preservation info box (blue-50 bg, blue-200 border):
      - AlertCircle icon
      - "Your data is preserved" heading
      - "Workspace mappings, user correlations, and historical data remain intact."
    - Footer: Cancel button (outline), Disconnect button (destructive, shows Loader2 when isDisconnecting)
    - Follow existing JiraDisconnectDialog pattern for Dialog structure
  </action>
  <verify>
    TypeScript compiles: `cd frontend && npx tsc --noEmit --skipLibCheck`
    Files exist with correct exports
  </verify>
  <done>
    AuthMethodBadge renders OAuth (blue) or API Token (gray) badges with icons.
    AuthMethodSwitchDialog displays confirmation with data preservation message.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add auth method badge and switch button to JiraConnectedCard</name>
  <files>frontend/src/app/integrations/components/JiraConnectedCard.tsx</files>
  <action>
    Update JiraConnectedCard:

    1. Add new props:
       - onSwitchAuth: () => void (callback when switch button clicked)

    2. Import AuthMethodBadge from "./AuthMethodBadge"

    3. Add AuthMethodBadge next to the title (after "Jira" span, before StatusIndicator):
       - Pass integration.token_source as authMethod
       - Badge is always visible (not in dropdown)

    4. Add "Switch to X" button in card footer (before disconnect button):
       - Only show when !hasTokenError (hide when token invalid)
       - Text: "Switch to {integration.token_source === 'oauth' ? 'API Token' : 'OAuth'}"
       - ArrowLeftRight icon (w-4 h-4 mr-2)
       - variant="outline" size="sm"
       - className: text-blue-600 border-blue-300 hover:bg-blue-50
       - onClick: onSwitchAuth
       - disabled: isLoading

    5. Add flex container for buttons in CardContent footer area:
       - Create div with "flex items-center justify-end space-x-2 pt-4 border-t mt-4"
       - Move disconnect button into this container
       - Add switch button before disconnect button

    Remove the "Auth Method" row from the details grid (it's now shown via badge).
  </action>
  <verify>
    TypeScript compiles: `cd frontend && npx tsc --noEmit --skipLibCheck`
    Component renders without errors
  </verify>
  <done>
    JiraConnectedCard displays auth method badge and "Switch to X" button.
    Switch button hidden when token has error.
    Button triggers onSwitchAuth callback.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add auth method badge and switch button to LinearConnectedCard</name>
  <files>frontend/src/app/integrations/components/LinearConnectedCard.tsx</files>
  <action>
    Update LinearConnectedCard (mirror Jira changes):

    1. Add new props:
       - onSwitchAuth: () => void (callback when switch button clicked)

    2. Import AuthMethodBadge from "./AuthMethodBadge"

    3. Add AuthMethodBadge next to the title (after "Linear" span, before StatusIndicator):
       - Pass integration.token_source as authMethod
       - Badge is always visible (not in dropdown)

    4. Add "Switch to X" button in card footer (before disconnect button):
       - Only show when !hasTokenError (hide when token invalid)
       - Text: "Switch to {integration.token_source === 'oauth' ? 'API Token' : 'OAuth'}"
       - ArrowLeftRight icon (w-4 h-4 mr-2)
       - variant="outline" size="sm"
       - className: text-blue-600 border-blue-300 hover:bg-blue-50
       - onClick: onSwitchAuth
       - disabled: isLoading

    5. Add flex container for buttons in CardContent footer area:
       - Create div with "flex items-center justify-end space-x-2 pt-4 border-t mt-4"
       - Move disconnect button into this container
       - Add switch button before disconnect button

    Remove the "Auth Method" row from the details grid (it's now shown via badge).
  </action>
  <verify>
    TypeScript compiles: `cd frontend && npx tsc --noEmit --skipLibCheck`
    Component renders without errors
  </verify>
  <done>
    LinearConnectedCard displays auth method badge and "Switch to X" button.
    Switch button hidden when token has error.
    Button triggers onSwitchAuth callback.
  </done>
</task>

</tasks>

<verification>
Run TypeScript check:
```bash
cd frontend && npx tsc --noEmit --skipLibCheck
```

Verify new files exist:
```bash
ls -la frontend/src/app/integrations/components/AuthMethodBadge.tsx
ls -la frontend/src/app/integrations/dialogs/AuthMethodSwitchDialog.tsx
```

Check imports work:
```bash
grep -l "AuthMethodBadge" frontend/src/app/integrations/components/*.tsx
```
</verification>

<success_criteria>
- AuthMethodBadge component created with OAuth (blue) and API Token (gray) variants
- AuthMethodSwitchDialog component created with data preservation messaging
- JiraConnectedCard shows auth method badge and switch button
- LinearConnectedCard shows auth method badge and switch button
- TypeScript compiles without errors
- Switch buttons are only visible when token is valid
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-experience/05-01-SUMMARY.md`
</output>
